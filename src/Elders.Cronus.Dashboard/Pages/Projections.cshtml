@page "/projections/"
@page "/projections/{Name}"

@if (string.IsNullOrEmpty(@Name))
{
    <ProjectionsList />
}
else
{
    <ProjectionInspect Name="@Name"></ProjectionInspect>
}

@functions{

    [Parameter]
    protected string Name { get; set; }
}
